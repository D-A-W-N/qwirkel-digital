# Desktop-Build-Matrix

## Ziel-Hosts

- macOS: für die aktuelle lokale Entwicklung und Release-Prüfung
- Linux: für CI- oder Linux-Host-Tests
- Windows: für CI- oder Windows-Host-Tests

## Aktueller Status

- Flutter-Desktop-Targets sind auf dem aktuellen Host noch nicht aktiviert
- Linux- und Windows-Builds sind auf macOS-Host nicht direkt ausführbar

## Nächste Schritte

1. Flutter Desktop-Targets auf allen Ziel-Hosts aktivieren
2. CI- oder lokale Build-Runner für Linux/Windows einrichten
3. Build- und Testlauf für alle Desktop-Targets automatisieren
4. Plattform-spezifische Distribution vorbereiten
